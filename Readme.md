# How it works


#### Firebase
* init project: `firebase init`
* view all the projects: `firebase projects:list`
* start emulator: `firebase emulators:start`


#### Dev monitor esp32
* install package: `python3 -m pip install -U platformio`
* start monitoring: `platformio device monitor`